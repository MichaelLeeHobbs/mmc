{
  "name": "mikes-mirth-code",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "rhino:compat": "node scripts/rhino-compat.mjs"
  },
  "devDependencies": {
    "vitest": "^2",
    "@vitest/coverage-v8": "^2"
  }
}
